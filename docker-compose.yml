version: '3.8'

services:
  face-recognition:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads
      - ./known_faces:/app/known_faces
      - ./data:/app/data
    restart: unless-stopped
